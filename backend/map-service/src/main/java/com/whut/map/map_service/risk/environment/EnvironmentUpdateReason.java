package com.whut.map.map_service.risk.environment;

public enum EnvironmentUpdateReason {
    WEATHER_UPDATED,
    WEATHER_EXPIRED,
    SAFETY_CONTOUR_UPDATED,
    SAFETY_CONTOUR_RESET,
    OWN_SHIP_ENV_REEVALUATED
}
